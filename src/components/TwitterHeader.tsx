import React from 'react';

interface AppProps {}

const TwitterHeader: React.FC<AppProps> = () => {
  return (
    <div className="">
      Twitter Header
    </div>
  );
}

export default TwitterHeader;
